pipeline {
  agent {
    kubernetes {
      cloud "openshift"
      label 'skopeo-podX'
    }
  }
  stages {
    stage('Run Skopeo X') {
      steps {
        container('skopeoX') {
          sh 'skopeo --version'
        }
      }
    }
  }
}