pipeline {
  agent {
    kubernetes {
      cloud "openshift"
      label 'skopeo-pod'
      defaultContainer 'skopeo'
      containerTemplate {
        name 'skopeo'
        image 'docker-registry.default.svc:5000/${GUID}-jenkins/jenkins-slave-appdev'
      }
    }
  }
  stages {
    stage('Run maven') {
      steps {
        container('skopeo') {
          sh 'skopeo --version'
        }
      }
    }
  }
}